[pytest]
# Coverage is configured here (rather than only documented in CI) so a
# plain `pytest` invocation from the repo root always reports coverage,
# keeping local runs and CI consistent. Tests import `backend.xxx`, so
# pytest must be invoked from the repo root (both locally and in CI).
addopts = --cov=backend --cov-report=term-missing
testpaths = backend/tests
